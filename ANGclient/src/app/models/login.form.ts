// Exporter une interface pour typer un objet
export interface LoginFormModel {
    errors: number,
    email: boolean,
    password: boolean,
    invalidUser: boolean,
    invalidPassword: boolean
};